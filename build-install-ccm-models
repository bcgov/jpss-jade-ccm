#!/bin/bash
set -e

echo "Building CCM Quarkus modules..."

# Always run from repo root
cd "$(dirname "$0")/.."

# Clean and build all modules without tests
mvn clean package -DskipTests

echo "CCM modules built successfully. No manual ccm-models.jar installation required."
