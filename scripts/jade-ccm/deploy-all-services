#!/bin/bash

echo "=========================================="
echo "Deploying ALL CCM Services"
echo "=========================================="

# Validate environment parameter
if [ -z "$1" ]; then
    echo "Error: Environment parameter required (dev, test, or prod)"
    exit 1
fi

ENV=$1

# Define all services
SERVICES=(
    "ccm-accessdedup-processor"
    "ccm-dems-adapter-quarkus"
    "ccm-justin-events-adapter-quarkus"
    "ccm-justin-in-adapter-quarkus"
    "ccm-justin-out-adapter-quarkus"
    "ccm-lookup-service-quarkus"
    "ccm-notification-service-quarkus"
    "ccm-pidp-adapter-quarkus"
    "ccm-reports-processor-quarkus"
    "ccm-splunk-adapter-quarkus"
)

SUCCESS_COUNT=0
FAILED_SERVICES=()

# Deploy each service
for SERVICE in "${SERVICES[@]}"; do
    echo ""
    echo "=========================================="
    echo "📦 Deploying: $SERVICE"
    echo "=========================================="

    SCRIPT="./redeploy-${SERVICE}"

    if [ -f "$SCRIPT" ]; then
        chmod +x "$SCRIPT"
        if "$SCRIPT" "$ENV"; then
            echo "✅ $SERVICE deployed successfully"
            ((SUCCESS_COUNT++))
        else
            echo "❌ $SERVICE deployment failed"
            FAILED_SERVICES+=("$SERVICE")
        fi
    else
        echo "⚠️  Script not found: $SCRIPT"
        FAILED_SERVICES+=("$SERVICE")
    fi
done

# Summary
echo ""
echo "=========================================="
echo "Deployment Summary"
echo "=========================================="
echo "✅ Successful: $SUCCESS_COUNT/${#SERVICES[@]}"
if [ ${#FAILED_SERVICES[@]} -gt 0 ]; then
    echo "❌ Failed services:"
    for SERVICE in "${FAILED_SERVICES[@]}"; do
        echo "   - $SERVICE"
    done
    exit 1
else
    echo "🎉 All services deployed successfully!"
fi
