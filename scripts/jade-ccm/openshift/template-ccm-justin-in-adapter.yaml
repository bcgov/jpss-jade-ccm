apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: deployment-template
parameters:
- name: NAMESPACE
  description: The namespace for the deployment
  required: true
objects:
- apiVersion: apps/v1
  kind: Deployment
  metadata:
    namespace: ${NAMESPACE}
    name: 'ccm-justin-in-adapter'
  spec:
    selector:
      matchLabels:
        app.kubernetes.io/name: ccm-justin-in-adapter  # Unique selector for the deployment
    replicas: 1
    template:
      metadata:
        labels:
          app: jade-ccm  # Existing label for broader grouping
          app.kubernetes.io/name: ccm-justin-in-adapter  # Unique label for this service
          pod-name: ccm-justin-in-adapter
        name: ccm-justin-in-adapter 
      spec:
        containers:
          - name: container
            image: >-
              image-registry.openshift-image-registry.svc:5000/${NAMESPACE}/ccm-justin-in-adapter:latest
            env:
              - name: 'TZ'
                value: 'Canada/Pacific'
              - name: 'KAFKA-BROKER-URL'
                value: events-kafka-bootstrap:9092
              - name: POD_NAME
                value: ccm-justin-in-adapter 
            envFrom:
              - configMapRef:
                  name: ccm-quarkus-configs
              - secretRef:
                  name: ccm-quarkus-secrets
        imagePullSecrets: []
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 25%
        maxUnavailable: 25%
    paused: false
- apiVersion: v1
  kind: Service
  metadata:
    namespace: ${NAMESPACE}
    name: ccm-justin-in-adapter 
  spec:
    selector:
      app.kubernetes.io/name: ccm-justin-in-adapter   # Match the unique label
    ports:
    - protocol: TCP
      port: 80
      targetPort: 8080  # Adjust to your container's port
    type: ClusterIP
- apiVersion: v1
  kind: Route
  metadata:
    namespace: ${NAMESPACE}
    name: ccm-justin-in-adapter 
  spec:
    host: jade-ccm-justin-adapter-dev.apps.silver.devops.gov.bc.ca
    to:
      kind: Service
      name: ccm-justin-in-adapter
    tls:
      termination: edge
      insecure-edge-termination-policy: Redirect