# Environment-specific values for ccm-justin-out-adapter in DEV
# This file extends values-ccm-justin-out-adapter.yml

# Multinode configuration for development environment
replicas: 2

# Override image repository for dev environment
image:
  repository: image-registry.apps.silver.devops.gov.bc.ca/cef5dd-dev/ccm-justin-out-adapter
  tag: latest
  pullPolicy: Always

# Pod anti-affinity to ensure pods are spread across different nodes
affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
    - weight: 100
      podAffinityTerm:
        labelSelector:
          matchExpressions:
          - key: app.kubernetes.io/name
            operator: In
            values:
            - ccm-app
        topologyKey: kubernetes.io/hostname

# Pod Disruption Budget - ensure at least 1 pod available during updates
podDisruptionBudget:
  enabled: true
  minAvailable: 1

# Horizontal Pod Autoscaler - automatically scale based on CPU/memory
autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80
