# Environment-specific values for ccm-justin-events-adapter in PROD
# This file extends values-ccm-justin-events-adapter.yml

# Single node configuration for production environment
replicas: 1

# No pod anti-affinity needed for single replica
# No PDB needed for single replica
