name: Deploy Quarkus App to OpenShift

on:
  workflow_dispatch:
    inputs:
      app:
        description: 'Select the application to deploy'
        required: true
        type: choice
        options:
          - ccm-justin-in-adapter
          - ccm-dems-adapter
          - ccm-lookup-service
          - ccm-splunk-adapter
          - ccm-accessdedup-processor
          - ccm-reports-processor
          - ccm-pidp-adapter
          - ccm-notification-service
          - ccm-justin-events-adapter
          - ccm-justin-out-adapter
          - redeploy-camel-simple
      env:
        description: 'Select the environment'
        required: true
        type: choice
        default: 'dev'
        options:
          - dev
          - test
          - prod

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.env }}

    env:
      APP: ${{ github.event.inputs.app }}
      ENV: ${{ github.event.inputs.env }}
      NS: cef5dd-${{ github.event.inputs.env }}
      OCP_SERVER: ${{ secrets.OCP_SERVER }}
      OCP_TOKEN: ${{ secrets.OCP_TOKEN }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Install OpenShift CLI (oc)
        run: |
          curl -L https://mirror.openshift.com/pub/openshift-v4/clients/ocp/latest/openshift-client-linux.tar.gz -o oc.tar.gz
          tar -xzf oc.tar.gz
          sudo mv oc /usr/local/bin/
          oc version --client

      - name: Login to OpenShift
        run: |
          oc login $OCP_SERVER --token=$OCP_TOKEN
          oc project $NS

      - name: Install Camel K CLI
        run: |
          curl -LO https://github.com/apache/camel-k/releases/download/v1.10.0/camel-k-client-1.10.0-linux-amd64.tar.gz
          tar -xzf camel-k-client-1.10.0-linux-amd64.tar.gz
          sudo mv kamel /usr/local/bin/
          kamel version

      - name: Build Quarkus app (redeploy-camel-simple only)
        if: env.APP == 'redeploy-camel-simple'
        working-directory: QuarkusInvestigation/camel-simple
        run: |
          chmod +x mvnw
          ./mvnw clean package -DskipTests -Dquarkus.kubernetes.deploy=true

      - name: Deploy Camel K integration (redeploy-camel-simple only)
        if: env.APP == 'redeploy-camel-simple'
        run: |
          kamel run QuarkusInvestigation/camel-simple/src/main/java/CamelSimple.java \
            --name=camel-simple \
            --namespace=$NS
