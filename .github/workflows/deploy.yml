name: Deploy to OpenShift

permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      app:
        description: 'Select the application to deploy'
        required: true
        type: choice
        options:
          - ccm-justin-in-adapter
          - ccm-dems-adapter
          - ccm-lookup-service
          - ccm-splunk-adapter
          - ccm-accessdedup-processor
          - ccm-reports-processor
          - ccm-pidp-adapter
          - ccm-notification-service
          - ccm-justin-events-adapter
          - ccm-justin-out-adapter
      env:
        description: 'Select the environment'
        required: true
        type: choice
        default: dev
        options:
          - dev
          - test
          - prod

jobs:
  deploy:
    name: Deploy ${{ github.event.inputs.app }} to ${{ github.event.inputs.env }}
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.env }}

    env:
      APP: ${{ github.event.inputs.app }}
      ENV: ${{ github.event.inputs.env }}
      OCP_SERVER: ${{ secrets.OCP_SERVER }}
      OCP_TOKEN: ${{ secrets.OCP_TOKEN }}
      OCP_PROJECT: cef5dd-${{ github.event.inputs.env }}

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Install OpenShift CLI (oc)
        run: |
          set -e
          echo "Installing OpenShift CLI..."
          curl -L https://mirror.openshift.com/pub/openshift-v4/clients/ocp/latest/openshift-client-linux.tar.gz -o oc.tar.gz
          tar -xzf oc.tar.gz
          sudo mv oc /usr/local/bin/
          rm -f oc.tar.gz
          oc version --client

      - name: 🔐 Login to OpenShift
        run: |
          set -e
          echo "Logging into OpenShift..."
          oc login "$OCP_SERVER" --token="$OCP_TOKEN"
          oc project "$OCP_PROJECT"

      # ------------------------------------------------------------
      # STEP 1: Build and install CCM models (so adapters can compile)
      # ------------------------------------------------------------
      - name: 🧱 Build and install CCM models
        run: |
          set -e
          echo "🏗️ Building CCM models using existing POM..."
          mvn -f pom.xml clean install -DskipTests

      # ------------------------------------------------------------
      # STEP 2: Build the selected app (ccm-xxx-adapter)
      # ------------------------------------------------------------
      - name: 🧩 Build selected Quarkus app
        run: |
          set -e
          echo "Building selected app: $APP ..."
          if [ ! -d "$APP" ]; then
            echo "❌ Directory for $APP not found!"
            exit 1
          fi
          if [ ! -f "$APP/pom.xml" ]; then
            echo "❌ No pom.xml found in $APP directory!"
            exit 1
          fi
          mvn -f "$APP/pom.xml" clean package -DskipTests
          echo "✅ Build successful for $APP"

      # ------------------------------------------------------------
      # STEP 3: Deploy the built app to OpenShift
      # ------------------------------------------------------------
      - name: 🚀 Deploy Quarkus Service
        run: |
          set -e
          echo "Checking for redeploy script: ./redeploy-$APP"
          if [[ -f "./redeploy-$APP" ]]; then
            echo "Found script: redeploy-$APP"
            chmod +x "./redeploy-$APP"
            ./redeploy-$APP "$ENV"
          else
            echo "❌ Redeploy script not found for $APP"
            exit 1
          fi

      # ------------------------------------------------------------
      # STEP 4: Final summary
      # ------------------------------------------------------------
      - name: ✅ Deployment Summary
        if: success()
        run: |
          echo "✅ Successfully built and deployed $APP to environment: $ENV (namespace: $OCP_PROJECT)"
