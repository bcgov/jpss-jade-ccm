name: Deploy to OpenShift

permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
    inputs:
      namespace:
        description: "Target OpenShift namespace"
        required: true
        type: choice
        options:
          - cef5dd-dev
          - cef5dd-test
          - cef5dd-prod
          - ac13ae-dev
          - ac13ae-test
          - ac13ae-prod

      app:
        description: "Application to deploy"
        required: true
        type: choice
        options:
          - ccm-justin-in-adapter
          - ccm-dems-adapter
          - ccm-lookup-service
          - ccm-splunk-adapter
          - ccm-accessdedup-processor
          - ccm-reports-processor
          - ccm-pidp-adapter
          - ccm-notification-service
          - ccm-justin-events-adapter
          - ccm-justin-out-adapter

jobs:
  deploy:
    name: Deploy to OpenShift
    runs-on: ubuntu-latest

    # 🔐 Environment == namespace
    environment: ${{ github.event.inputs.namespace }}

    env:
      NAMESPACE: ${{ github.event.inputs.namespace }}
      APP: ${{ github.event.inputs.app }}
      CI: true
      AUTO_APPROVE: "true"

      # Secrets come from the environment
      OCP_SERVER: ${{ secrets.OCP_SERVER }}
      OCP_TOKEN: ${{ secrets.OCP_TOKEN }}
      SA_REGISTRY_PASSWORD: ${{ secrets.OC_REG_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install OpenShift CLI
        run: |
          curl -L https://mirror.openshift.com/pub/openshift-v4/clients/ocp/latest/openshift-client-linux.tar.gz -o oc.tar.gz
          tar -xzf oc.tar.gz
          sudo mv oc /usr/local/bin/oc
          oc version --client

      - name: Validate OpenShift credentials
        run: |
          if [[ -z "$OCP_SERVER" || -z "$OCP_TOKEN" ]]; then
            echo "❌ Missing OpenShift credentials for environment: $NAMESPACE"
            exit 1
          fi

      - name: Login to OpenShift
        run: |
          echo "Logging into OpenShift API..."
          oc login "$OCP_SERVER" --token="$OCP_TOKEN"
      
      - name: Switch to OpenShift project
        run: |
          echo "Using project $NAMESPACE"
          oc project "$NAMESPACE"

      - name: Set Registry Host
        run: |
          if [[ "$NAMESPACE" == cef5dd-* ]]; then
            echo "REGISTRY_HOST=image-registry.apps.silver.devops.gov.bc.ca" >> $GITHUB_ENV
          elif [[ "$NAMESPACE" == ac13ae-* ]]; then
            echo "REGISTRY_HOST=image-registry.apps.clab.devops.gov.bc.ca" >> $GITHUB_ENV
          else
            echo "❌ Unsupported namespace: $NAMESPACE"
            exit 1
          fi

      - name: Login to OpenShift Image Registry
        run: |
          echo "$SA_REGISTRY_PASSWORD" | docker login \
            -u github-deployer \
            --password-stdin \
            "$REGISTRY_HOST"

      - name: Install Camel K CLI
        run: |
          curl -Lo kamel https://github.com/apache/camel-k/releases/download/v1.10.0/camel-k-client-1.10.0-linux-64bit
          chmod +x kamel
          sudo mv kamel /usr/local/bin/kamel

      - name: Build CCM models jar
        run: |
          chmod +x ./repackage-ccm-models
          ./repackage-ccm-models

      - name: Derive environment from namespace
        run: |
          ENV="${NAMESPACE##*-}"
          echo "ENV=$ENV" >> $GITHUB_ENV
            
      - name: Deploy application
        shell: bash
        run: |
          set -euo pipefail
      
          cd scripts/jade-ccm
      
          # ------------------------------------------------------------------
          # Normalize variables for legacy redeploy scripts
          # ------------------------------------------------------------------
      
          # ENV must be dev|test|prod
          export ENV="${ENV}"
      
          # NAMESPACE already provided (example: cef5dd-dev)
          export NAMESPACE="${NAMESPACE}"
      
          # PROJECT is expected by redeploy scripts (example: cef5dd)
          export PROJECT="${NAMESPACE%-*}"
      
          # Common aliases used across scripts
          export OPENSHIFT_PROJECT="${PROJECT}"
          export OCP_PROJECT="${PROJECT}"
          export OS_PROJECT="${PROJECT}"
      
          # Safety checks
          if [[ -z "$ENV" || -z "$PROJECT" || -z "$NAMESPACE" ]]; then
            echo "❌ Invalid deployment context"
            echo "ENV=$ENV"
            echo "PROJECT=$PROJECT"
            echo "NAMESPACE=$NAMESPACE"
            exit 1
          fi
      
          echo "🚀 Deploying application"
          echo "  App:        $APP"
          echo "  Namespace:  $NAMESPACE"
          echo "  Project:    $PROJECT"
          echo "  Environment:$ENV"
      
          # ------------------------------------------------------------------
          # Execute redeploy script
          # ------------------------------------------------------------------
      
          SCRIPT="redeploy-${APP}"
      
          if [[ ! -f "$SCRIPT" ]]; then
            echo "❌ Deployment script not found: $SCRIPT"
            exit 1
          fi
      
          chmod +x "$SCRIPT"
      
          # Pass ENV explicitly (legacy scripts expect $1)
          ./"$SCRIPT" "$ENV"
      
